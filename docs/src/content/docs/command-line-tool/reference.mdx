---
title: Reference
description: All of the functionality of the resumay command line tool
---

## Commands

### `create`

The create command is used to generate new output.

#### `resume`

The resume subcommand creates a PDF resume from a config and template file. By default assumes the files are named `resume.toml` and `template.html`

## Flags
